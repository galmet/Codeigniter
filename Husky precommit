#!/bin/sh

echo "🔍 [Windows Git] Intercepting hook. Forwarding linting execution to WSL..."

# Force Windows to run the linter inside WSL via bash, passing the environment paths correctly
wsl.exe bash -c "cd \$(wslpath '$PWD') && npx --no-install lint-staged"
