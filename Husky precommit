#!/bin/sh

echo "🔍 [Windows Git] Forwarding execution to WSL environment..."

# -i forces an interactive login shell, loading all your Node/NVM pathways natively inside WSL
wsl.exe bash -ic "cd \$(wslpath '$PWD') && ./node_modules/.bin/lint-staged"
